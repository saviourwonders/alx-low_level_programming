#include "main.h"

/**
 * _isdigit - checks for digits (0 - 9)
 * @c: digit
 *
 * Return: 1 for any digit and 0 otherwise
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}

	else
	{
		return (0);
	}

	return (0);
}
